# 坤发卡 · 全自动发卡系统(开源版)

> PHP + MySQL 开发, 专为**宝塔面板**环境设计, 傻瓜式安装向导, 支付成功**秒级自动发卡**。
> 无框架/无 Composer 依赖, 单包上传即用; 内置应用商店(插件 + 主题)、多套主题、6大支付通道, 桌面/平板/手机全端适配。

## ✨ 特性一览

- 🚀 **全自动发卡**: 支付成功 → 秒级发卡 → 卡密页展示 + 邮件同步备份, 全天候无人值守
- 💳 **6大支付通道**: 码支付/易支付(聚合, 支持多渠道勾选: 支付宝/微信/QQ同时开放)、支付宝当面付、支付宝官方、微信Native、USDT-TRC20免挂机(唯一尾数金额自动识别到账)、国际信用卡 Visa/Mastercard(Stripe Checkout 官方协议, Webhook验签+回跳双通道发货)
- 🛒 **应用商店**: 插件/主题双板块, 演示截图陈列, 支持远程市场上架分发; 付费应用凭激活码(一码一站)解锁
- 🎨 **5套主题**: 云商城(默认, 亮/暗双模式)/二次元/樱花季/赛博暗黑/极简白, 后台一键切换, 全部全端自适应
- 👥 **前台会员**: 注册/登录/我的订单, 公告系统(多条+公告页), 自定义单页(联系方式/购买须知)
- 🛡️ **安全内置**: CSRF全站防护、注册每IP限3个/日、登录错5次锁60分钟、人机验证三选一(图形验证码/Turnstile/极验v4)、高熵防枚举订单号、支付回调验签+金额比对、Zip Slip防护、SMTP头注入防护
- 🌐 **CDN友好**: 未套CDN/Cloudflare/其他CDN三种接入模式, 真实IP识别正确, 动态页禁缓存防"支付不回调"
- 🔐 **随机后台入口**: 安装时自动生成随机管理地址(旧 admin.php 失效), 防扫描
- ⚙️ **运维顺手**: 系统日志+一键清理、订单一键清理、卡密批量导入、库存预警、周销售趋势

## 📦 安装(3分钟, 宝塔)

| 项目 | 要求 |
|---|---|
| Web服务器 | Nginx / Apache |
| PHP | 7.4 ~ 8.3 |
| PHP扩展 | pdo_mysql、curl、openssl、zip、bcmath、gd |
| MySQL | 5.6 / 5.7 / 8.0 或 MariaDB |

1. 宝塔建站(PHP 7.4+, 记住数据库密码)
2. 上传本仓库代码(或 Release 的zip)到站点根目录解压
3. 访问域名 → 自动进入安装向导 → 填数据库信息 + 设置管理员账号 → 完成
4. **立即收藏安装完成页展示的随机后台入口**(形如 `admin_x7k9q2.php`, 仅展示一次)

详细文档(支付通道配置/主题开发/CDN配置/常见问题)见下文章节。

## 🗺️ 版本演进

> v1.1.0 前台会员系统、注册IP限制与登录爆破锁定。
> v1.2.0 人机验证三选一 —— 极验GeeTest v4 / Cloudflare Turnstile / 图形验证码。
> v1.9.0 全新默认主题「云商城」, 亮/暗双模式一键切换。
> v1.10.0 公告系统(多条+公告页) + 自定义单页。
> v1.12.0 专业版自助购买(码支付多渠道 + USDT)。
> v1.13.0 码支付/易支付**多支付渠道**勾选, 前台按勾选展开多个带品牌图标支付方式。
> v2.1.0 **国际信用卡(Visa/Mastercard)**, Stripe Checkout 官方协议对接, 双通道发货。
> v2.2.0 **CDN接入模式**(真实IP识别/禁缓存/回调配置指引)。
> v2.3.x 安装器输入白名单、Location头CRLF清洗等安全加固。
> v2.4.0 后台UI v6「净简」纯中性灰阶改版, 系统设置标签分页。
> v2.11.0 卡密管理改版(参考主流发卡面板): 上传卡密弹窗(商品/备注/去重开关) + 锁定/解锁/标记已售/移除批量操作 + 精确与模糊搜索 + 入库时间范围筛选 + 未出售/已出售/已锁定页签 + CSV导出; 卡密新增note备注列(老站用tools/upgrade.php升级)。
> v2.10.0 商品管理改版(参考主流发卡面板): 四栏统计卡(总商品/已上架/未上架/总库存) + 分类/名称筛选与上下架页签 + 右侧抽屉式添加/编辑(基本信息/商品介绍/购买限制/卡密导入四页签) + 行内上下架开关 + 分时段销量列(今日/昨日/本周/累计) + 批量上架/下架/移除。
> v2.9.0 分类管理改版(参考主流发卡面板): 添加/编辑弹窗(名称/图标/排序/启停) + 行内启停开关 + 批量启用/停用/移除 + 前台链接复制 + 名称筛选与启停页签; 新增categories.status列(停用分类前台隐藏, 老站用tools/upgrade.php升级)。
> v2.8.0 会员管理改版(参考主流发卡面板): 四栏统计卡(总用户/今日新增/已封禁/会员成交订单) + 用户名/UID/邮箱/注册IP筛选 + 全部/正常/封禁页签 + 勾选批量启用/禁用/移除。
> v2.7.0 控制台改版(参考主流发卡面板): 销售额三卡(今日/昨日/本月+同期对比) + 待处理事项行 + 7/30天成交趋势图(销售额/订单数切换, 纯CSS无依赖) + 多时段经营数据(今日/昨日/本周/本月/全部) + 站内公告与登录信息卡。
> v2.6.0 商品订单中心改版(参考主流发卡面板): 三栏实时统计卡(随筛选联动) + 多条件筛选(订单号/商品ID/卡密/联系方式/支付方式/IP/会员ID/时间范围) + 快捷状态页签 + CSV导出 + 勾选批量销毁 + 一键清理。
> v2.5.0 **激活码授权**: 移除官方账号体系, 授权中心填邮箱直购(码支付/USDT) → 主控自动生成永久激活码(页面+邮件双送达) → 一键激活; 一码仅绑定一个站点。

## 二、安装步骤(3分钟)

1. **宝塔 → 网站 → 添加站点**: 域名填你的域名, PHP版本选 7.4+, 数据库选 MySQL 并记住密码。
2. **上传程序**: 把 `坤发卡.zip` 上传到站点根目录并**解压到当前目录**(解压后站点根目录下直接是 index.php 等文件)。
3. **访问域名**: 浏览器直接打开 `http://你的域名/` —— 未安装时自动跳转到安装向导
   - 第1步环境检测全部通过 → 第2步同意协议 → 第3步照抄宝塔数据库页面的信息 + 设置管理员账号密码 → 完成。
4. **收藏后台地址**: 安装完成页面会展示**随机生成的后台安全入口**(形如 `http://你的域名/admin_x7k9q2.php`), 该地址只在此时完整展示一次, 请立即收藏!
   - 原始的 `admin.php` 在安装时会被自动重命名, 旧的 `admin.php` 地址访问返回404。
   - 如果忘了后台地址: FTP查看 `data/config.php` 中的 `YF_ADMIN_ENTRY` 即可找到。
5. **安全收尾**: 删除 `install.php`; Nginx用户在宝塔站点配置中加入:

```nginx
location ^~ /data/ { deny all; }
```

## 三、老版本升级(v1.0.0 / v1.1.0 → v1.2.0)

已安装老版本的站点: 浏览器访问一次 `http://你的域名/tools/upgrade.php` 即可自动补齐
新表(会员/注册记录/登录失败记录)和新配置项(验证方式/极验配置等), 幂等安全。完成后删除该文件。
全新安装无需此步骤。

## 四、配置计划任务(重要)

宝塔 → 计划任务 → 添加任务:
- 类型: **Shell脚本**, 执行周期: **每1分钟**, 脚本内容:

```bash
php /www/wwwroot/你的站点目录/cron.php
```

也可以用访问URL方式(需携带密钥, 密钥见 `data/config.php` 中的 `YF_KEY`):

```bash
curl -s "http://你的域名/cron.php?key=你的YF_KEY值"
```

作用: ① 未支付订单超时自动关闭 ② USDT免挂支付链上到账轮询(买家支付页也会自动触发检测, 配置计划任务实时性更好)。

## 五、快速上手流程

1. **后台 → 分类管理**: 添加分类(如 游戏充值/软件激活码)。
2. **后台 → 商品管理**: 新增商品, 填价格、介绍。
3. **后台 → 卡密库存**: 选择商品, 每行一张批量导入卡密(支持 `卡号----密码` 任意格式)。
4. **后台 → 应用商店 → 插件**: 安装并**启用**支付插件(见下文配置方法)。
5. **买家访问首页** → 选购 → 支付 → 系统自动检测到账 → **卡密立即自动展示并发送到邮箱**。

### 会员系统(v1.1.0)
- 前台支持会员**注册/登录**(右上角入口), 登录后可查看「我的订单」, 下单自动关联账号、联系方式自动预填, 卡密同步发到注册邮箱。
- 后台新增「会员管理」: 查看/搜索会员、禁用/启用、删除(历史订单保留)。

### 下单联系方式(v1.4.0)
- 后台「系统设置 → 下单联系方式」可勾选开放哪些类型: **Telegram / 邮箱 / QQ / 微信 / 电话**, 支持**多选**或**只留一个**(至少一项)。
- 买家购买页以胶囊单选形式选择类型, 输入框占位提示随类型变化, 并按类型做格式校验(邮箱/手机号/QQ号/TG用户名/微信号)。
- 订单列表、订单详情、订单结果页均显示联系方式类型; 仅「邮箱」类型的订单会自动发送卡密邮件。

### 客服联系方式(v1.7.0)
- 后台「系统设置 → 客服联系方式」可添加多条客服: **Telegram / 邮箱 / QQ / 微信 / 电话** + 备注(如「售后」「工作时间」), 可随时增删。
- 前台全站右下角显示**悬浮客服按钮**, 点击弹出客服面板: 微信/QQ一键复制账号, Telegram直达t.me链接, 邮箱/电话直达拨打; 未配置时不显示按钮。

### 系统日志与一键清理(v1.5.0)
- 后台新增「📜 系统日志」: 自动记录管理员/会员登录、支付发货、USDT链上到账、商店安装/主题切换、清理操作等关键事件, 支持按类型筛选。
- 日志页**一键清空**; 订单页提供**一键清理**: 清理已过期订单 / 清理待支付订单 / 清空全部订单(均需二次确认, 清理动作本身会写入日志留痕)。

### 人机验证(极验GeeTest / Cloudflare Turnstile / 图形验证码)
- 后台「系统设置 → 人机验证」三选一, 作用于: **前台注册、前台登录、后台登录**。
- **极验 GeeTest v4**(推荐, 一键通过体验): 到 [极验后台](https://auth.geetest.com) 创建行为验证4.0验证单元, 把 Captcha ID / Captcha Key 填入后台即可。
  - 「验证有效期(秒)」默认 **120**, 可在后台调整(10-600): 弹窗验证通过后, 超出该时限的验证结果会被服务端二次校验拒绝; 同时作为前端验证请求超时。
  - 采用bind模式: 点击登录/注册按钮弹出极验窗口, 低风险用户**一键通过**, 高风险自动升级滑块/图标等挑战, 服务端再做二次校验(签名: HMAC-SHA256)。
- **Cloudflare Turnstile**: 到 [Cloudflare Dashboard → Turnstile](https://dash.cloudflare.com/?to=/:account/turnstile) 添加站点, 填入 Site Key / Secret Key。
- 未开启上述任一验证时自动使用**内置图形验证码**(GD生成, 点击刷新); 也可整体关闭验证。
- 任一模式的密钥填写不完整时, 系统自动降级为图形验证码, 不会锁死登录。

### 安全限制(自动生效, 无需配置)
| 防护项 | 规则 |
|---|---|
| 注册IP限制 | 同一IP每24小时最多注册 **3** 个账号, 超出后需等待窗口滚动(最长24小时) |
| 登录爆破锁定 | 同一账号+IP 一天内密码错误 **5** 次 → 锁定 **60分钟**(锁定期间正确密码也无法登录) |
| IP轮换爆破兜底 | 同一IP 60分钟内累计错误 ≥20 次 → 锁定60分钟 |

### 安全设计(v1.8.0 渗透测试加固)
- **订单号高熵随机**(64位随机空间), 无法遍历枚举他人订单与卡密;
- 全站表单**CSRF防护**(前台下单/登录/注册/支付 + 后台全部操作), 外部支付回调按签名校验不受影响;
- 支付回调**双重校验**: 签名验证 + **金额与订单一致性比对**, 异常金额自动拒绝并写入日志;
- 计划任务URL方式需携带密钥 `cron.php?key=YF_KEY`(Shell方式不受限), 防止外部随意触发;
- 应用商店安装包**Zip Slip防护**, 拒绝路径穿越的恶意包; 插件类名白名单校验;
- 系统安装器防重装接管(已有管理员时拒绝重装, 即使install.lock被删);
- 自动携带安全响应头(nosniff / X-Frame-Options / Referrer-Policy)。
- 渗透测试覆盖: SQL注入/XSS反射与存储/CSRF/越权枚举/通知伪造/金额篡改/路径穿越/cron滥用/敏感文件/爆破锁定, 共32项断言全部通过。

## 六、支付插件配置

后台 → 应用商店 → 插件 → 安装/启用 → 配置:

> 支付选择页与支付页会自动显示品牌图标: 微信(绿)/支付宝(蓝)/USDT(泰达币)/QQ等; 码支付、易支付等聚合插件按其配置的「支付渠道」自动匹配对应品牌图标, 未配置时显示通用钱包图标。

### 1. 码支付(易支付协议)
- 平台: [xpay.shw1.com](https://xpay.shw1.com/doc/epay_submit)(接口文档)
- 填写: 网关(默认已填)、商户ID(pid)、商户密钥(KEY)、支付渠道。

### 2. 易支付
- 填写任意易支付平台的 网关地址/商户ID/商户密钥。

### 3. 支付宝当面付 / 支付宝官方
- [支付宝开放平台](https://open.alipay.com) 创建应用 → 签约「当面付」或「电脑网站支付」。
- 拿到: APPID、应用私钥(PKCS8)、支付宝公钥, 填入插件配置。
- 回调地址系统已自动生成, 无需手填。

### 4. 微信官方支付(V3 Native扫码)
- [微信支付商户平台](https://pay.weixin.qq.com) 开通 Native 支付产品。
- 拿到: 商户号、APPID(需与商户号绑定)、API证书序列号、`apiclient_key.pem` 私钥内容、APIv3密钥。

### 5. USDT免挂支付(TRC20) ⭐ 专业版专享
- 应用商店下载启用(需专业版授权)。
- 配置中只需填一项: **你的TRC20钱包地址**(以T开头)。
- 原理: 每笔订单分配唯一金额尾数, 系统通过TRON公链公开接口(TronGrid)自动识别到账并自动发货。无需挂机软件、无需节点、无需API密钥。

## 七、应用商店 & 专业版授权

- 商店分 **插件** / **主题** 两个板块; 应用分免费和付费。
- 免费应用: 无需任何账号, 直接下载安装。
- 付费应用: 激活专业版授权后免费下载, 后续上架的新付费应用同样免费。
- 后台 → 授权中心: 填邮箱发起购买 → 跳转官方收银台(码支付/USDT) → 支付成功自动生成**永久激活码**(页面展示+邮件发送) → 一键激活; 也可直接粘贴已有授权码(支持离线校验)。**一码仅绑定一个站点**。
- 本仓库为**发卡端本体**: 市场服务端(应用分发/授权码销售/在线收款)由官方运营, 不随本仓库发布; 商店协议(购买/查单/激活/列表/下载)均为开放的 HTTPS+JSON 接口, 兼容任何实现该协议的市场服务端。

## 八、主题

后台 → 应用商店 → 主题 → 「使用该主题」一键切换:

| 主题 | 风格 | 权限 |
|---|---|---|
| 云商城(默认) | 现代商城 · 浅灰底白卡片 + 深色Hero横幅 + 彩色分类卡, 亮/暗一键切换 | 免费 |
| 初音物语 | 二次元动漫 · 粉蓝渐变 + 樱花飘落动效 | 免费 |
| 樱花季 | 二次元少女 · 通体樱花粉 | 免费 |
| 赛博暗黑 | 深色科技 · 霓虹光效 | 专业版专享 |
| 极简白 | 简约清爽 · 去动效 | 免费 |

自定义主题: 复制 `themes/store` 改CSS即可, 视图自动继承(缺失视图回退默认主题)。

## 九、目录结构

```
坤发卡.zip (解压到站点根目录, 文件直接位于根级)
├── index.php            # 前台入口(未安装时自动跳转安装向导)
├── admin.php            # 后台入口(安装时自动重命名为随机安全入口)
├── cron.php             # 计划任务(订单过期+USDT轮询)
├── install.php          # 安装向导(装完可删)
├── database.sql         # 数据库结构
├── app/                 # 核心框架/控制器/库类
├── plugins/             # 支付插件(可扩展)
├── themes/              # 主题(可扩展)
├── assets/              # 公共静态资源
├── data/                # 配置与运行时(禁止公开访问)
└── tools/               # 打包/升级工具
```

## 十、常见问题

- **页面空白/报错**: 确认PHP≥7.4, 宝塔站点 → 设置 → PHP版本。
- **支付回调不成功**: 后台系统设置里把「网站地址」填为完整域名(https://...), 保证公网可访问回调地址。
- **USDT不到账**: 检查钱包地址是TRC20(T开头); 确认买家转账金额与页面显示完全一致; 配置了每分钟cron.php计划任务。
- **微信/支付宝提示验签失败**: 支付宝公钥/应用私钥填反了; 微信APIv3密钥或证书序列号填错。
- **极验弹窗不出现**: 检查Captcha ID/Key是否填反; 极验后台若设置了域名白名单需包含你的域名; 浏览器控制台确认 gt4.js 可访问。
- **商店无法下载远程应用**: 检查「系统设置→官方市场地址」是否可访问, 授权中心是否已登录。
